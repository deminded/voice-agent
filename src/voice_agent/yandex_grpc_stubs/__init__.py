"""Yandex SpeechKit v3 gRPC stubs.

Generated from yandex-cloud/cloudapi. The generated files use absolute
'yandex.cloud.*' imports, so we inject this directory into sys.path once
to let Python resolve them without a top-level 'yandex' package conflict.
"""
import sys
from pathlib import Path

_stubs_dir = str(Path(__file__).parent)
if _stubs_dir not in sys.path:
    sys.path.insert(0, _stubs_dir)
